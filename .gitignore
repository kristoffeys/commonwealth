# dependencies
node_modules/
.pnpm-store/

# build output
dist/
*.tsbuildinfo

# vendored plugin runtime (platform-local; rebuild with packages/plugin/scripts/bundle.mjs)
packages/plugin/vendor/

# test / coverage
coverage/

# derived brain index (rebuildable from markdown — never committed)
index/
*.db
*.db-shm
*.db-wal

# env / local
.env
.env.*
!.env.example
.DS_Store

# editor
.idea/
.vscode/*
!.vscode/extensions.json

# local Claude Code settings (personal, per-clone)
.claude/settings.local.json

# Stray brain state if commonwealth init/tests run inside this source repo
.commonwealth/
staging/
COMMONWEALTH.md
