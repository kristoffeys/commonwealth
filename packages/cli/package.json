{
  "name": "@commons/cli",
  "version": "0.0.0",
  "description": "The unified `commons` CLI — starting with the zero-config `init` onboarding wizard",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "commons": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@commons/core": "workspace:*",
    "@commons/curate": "workspace:*",
    "@commons/seed": "workspace:*"
  }
}
